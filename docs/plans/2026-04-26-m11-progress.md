# M11 交付记录（分享人主页链路）

## 已完成
- 新增分享人主页能力：
  - 接口常量：`/user/{id}/share_articles/{page}/json`
  - 数据层：`UserShareRepository`
  - 状态层：`UserShareViewModel`
  - 页面：`UserSharePage`

## 页面接入
- 广场列表项新增“分享人主页”入口（有 `userId` 时显示）。
- 问答列表项新增“分享人主页”入口（有 `userId` 时显示）。
- `AppShellPage` 新增页面路由状态：
  - 可进入 `UserSharePage`
  - 可从该页返回并继续打开文章详情。

## 影响文件
- `common/network/WanAndroidApi.ets`
- `features/plaza/PlazaRepository.ets`
- `features/wenda/WendaRepository.ets`
- `features/user/*`
- `features/plaza/PlazaPage.ets`
- `features/wenda/WendaPage.ets`
- `app/AppShellPage.ets`
