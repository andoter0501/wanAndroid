# M6 交付记录（稳定性 + 回归加固）

## 已完成
- 会话失效全局联动：
  - `ApiClient` 遇到 `-1001` 时清会话并广播事件。
  - `AppShellPage` 监听会话失效后自动回到“我的”页并给出提示。
- 搜索历史持久化（轻量）：
  - `SearchViewModel` 使用 `LocalCacheStore` 保存最近 10 条历史。
- 消息能力增强：
  - 未读/已读列表支持分页加载更多。
- 收藏能力增强：
  - 我的收藏列表支持“取消收藏”。

## 质量资产
- 新增会话事件总线：`common/session/SessionEventBus.ets`
- 新增本地缓存骨架：`common/storage/LocalCacheStore.ets`
- 新增 API 烟雾检查脚本：`scripts/smoke/api_smoke_check.sh`
- 新增测试与发布检查清单：`docs/tests/release-quality-checklist.md`
