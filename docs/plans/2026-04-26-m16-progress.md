# M16 交付记录（弱网重试策略）

## 已完成
- `ApiClient` 增加统一重试能力：
  - GET 请求默认重试 1 次（可通过 `retryCount` 覆盖）。
  - 对网络抖动、`5xx`、`408`、`429` 进行有限重试。
  - POST 默认不重试，避免重复提交风险。
- 保持鉴权安全：
  - `-1001`（会话失效）不参与重试，仍立即触发会话清理与事件广播。

## 实现位置
- `common/network/ApiClient.ets`
