# M7 交付记录（统一列表组件与文案）

## 已完成
- 新增统一文案常量：
  - `common/ui/AppText.ets`
  - 状态页默认文案集中管理（加载、空态、错误、重试、加载更多）。
- 新增统一分页列表容器：
  - `common/ui/components/PagedListView.ets`
  - 封装 `StateView + List + 加载更多` 的重复模板。

## 页面收敛结果
- 已切换到 `PagedListView`：
  - 首页列表
  - 搜索结果列表
  - 发现-项目列表
  - 广场列表
  - 我的分享列表
  - 问答列表
  - 我的-收藏列表
  - 我的-消息列表（未读/已读）

## 价值
- 统一了“状态渲染 + 分页加载”行为，后续维护点显著减少。
- 统一默认文案后，页面体验一致性更高，减少散落硬编码。
