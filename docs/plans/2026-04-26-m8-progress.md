# M8 交付记录（加载骨架 + 页面切换动效）

## 已完成
- 加载态视觉升级：
  - `StateView` 默认 Loading 从单一转圈升级为骨架条 + 文案。
  - 统一页面在加载时的感知一致性，减轻“空白等待”。
- 页面切换动效：
  - `AppShellPage` 中 Tab 切换加入轻量动效。
  - 首页进入搜索、搜索返回、Web 详情进出均加入过渡动效。

## 影响范围
- 所有使用 `StateView` 与 `PagedListView` 的页面自动继承上述体验优化。
